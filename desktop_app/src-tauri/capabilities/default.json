{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability set for the main application window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
