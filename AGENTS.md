# 拼豆图纸工作流程

本文是本仓库的强制工作约定. 新智能体在处理任何图片转拼豆任务前, 必须先阅读本文和对应项目目录内的 `README.md`.

## 1. 默认约定

- 用户未指定品牌或色卡时, 默认使用 MARD 色号标准.
- 本仓库当前的默认色卡是 `palettes/mard_221_v1.json`.
- 使用固定品牌色卡时, 图纸, 色号表和材料统计中的 RGB 与色号必须直接来自该色卡. 禁止生成不存在于色卡中的近似色号.
- 用户未指定尺寸时, 根据主体细节给出建议尺寸. 用户指定板子或占用范围时, 成品必须完全落在该范围内.
- 不得把原图直接缩小后当作拼豆图纸. 必须先在较高分辨率下整理构图和结构, 再栅格化和量化.
- 不得覆盖已经交付的版本. 新修改写入新的变体目录, 并在项目 `README.md` 中标出推荐版本.

## 2. 目录约定

每个对象放在独立目录中:

```text
projects/<name>/
├── README.md
├── masters/     # 去背景和简化后的高分辨率母图
├── patterns/
│   └── <name>_<width>x<height>_<version>/
└── archives/    # 交付压缩包
```

通用文件放置规则:

- 图纸标识必须使用 `<name>_<size>_<version>`.
- `name` 是图片主体的真实名称, 使用小写 snake_case. 例如 `tiamat_emblem` 和 `tempus_emblem`.
- `size` 是最终色号网格中所有非空拼豆格的最小外接矩形, 按横向占用格数 x 纵向占用格数记录. 它不是画布尺寸, 拼板尺寸或用户给出的最大尺寸.
- `version` 使用按历史迭代顺序递增的 `vN`, 例如 `v1` 和 `v2`.
- 图纸目录, CSV, 预览图, 坐标图, 材料表, 元数据, 分板图和压缩包都使用同一个图纸标识作为文件名前缀.
- `README.md` 和 `tiles/` 是固定职责名称, 不使用图纸标识. `masters/` 中的高分辨率母图尚无唯一拼豆外接矩形, 可以使用语义化文件名.
- 转换脚本放在 `scripts/`.
- 品牌色卡放在 `palettes/`.
- 临时文件不得混入最终图纸目录.
- 每个 `patterns/<name>_<size>_<version>/` 应当是可单独交付和复现的版本.

## 3. 转换流程

### 3.1 检查原图和目标约束

开始编辑前记录以下内容:

1. 原图尺寸, 色彩模式和透明通道状态.
2. 主体范围, 背景, 外包边, 内部结构线, 细长结构和孔洞.
3. 是否存在轴对称或中心对称结构.
4. 目标板尺寸, 最大占用宽高和拼接方式.
5. 指定色卡. 未指定时记为 `MARD 221 v1`.
6. 必须保留的识别特征, 以及可以删除或合并的细节.

如果用户的尺寸要求和必须保留的结构明显冲突, 应先说明最小可行尺寸, 不得静默牺牲关键结构.

### 3.2 制作高分辨率母图

先在原图分辨率或足够高的工作分辨率下完成:

- 删除背景并输出带 alpha 的 PNG.
- 删除噪点, 纹理, 复杂反光和无意义的小阴影.
- 合并过细的发丝, 链条, 划痕和装饰线.
- 简化线条和阴影, 必要时卡通化, 但保留轮廓比例和核心识别特征.
- 把必须存在的细长结构加宽到目标网格至少 1 格, 重要边界通常至少 2格.
- 先修正武器, 五官, 眼镜, 手指, 环和孔洞等结构关系, 再进入降采样.

如果使用图像生成或图像编辑模型, 其输出只作为母图. 不要求模型直接生成像素画或带网格的拼豆图. 每次迭代只修改少量明确问题, 并在项目记录中保存提示词或修改说明.

### 3.3 包边处理

先区分外包边和内部结构线:

- 仅沿主体外轮廓向外扩张的包边, 在转化前去除. 这类包边不能占用拼豆格, 也不能因为去除方式错误而缩小主体.
- 包边如果延伸到图片内部, 并承担材质分界, 刀刃, 缝隙, 五官或其他结构表达, 只保留位于内部且确有结构意义的部分.
- 不得把所有深色线统一当作包边删除.
- 不得默认给最终图纸重新增加一圈深色外轮廓. 只有用户明确要求时才添加.

去除后检查 alpha 边缘. 外轮廓之外应透明, 主体边界应清晰, 不得残留彩边, 半透明毛边或孤立暗色格.

### 3.4 对称结构

需要识别并保持以下对称关系:

- 竖直, 水平或斜向轴对称.
- 绕中心旋转 $180^\circ$ 后重合的中心对称.

处理规则:

1. 在母图阶段先校正几何中心和对称轴.
2. 对需要单格中心线或尖头的图案, 优先使用奇数占用宽度或奇数占用高度.
3. 即使板子尺寸是偶数, 主体也可以围绕某一实际格列或格行对称, 但必须在元数据中记录对称轴位置.
4. 色彩量化和清理完成后再次强制对称. 不得只依赖母图视觉上近似对称.
5. 轴对称时, 镜像格必须具有相同的占用状态和色号. 中心对称时, 对应格 $(r,c)$ 与 $(2r_0-r,2c_0-c)$ 必须一致.
6. 用户未明确要求保留非对称光照时, 对称结构的几何和颜色都应对称.

如果来源两侧细节不同, 应先依据结构语义决定左侧或右侧作为基准, 再镜像. 不得逐格随意取舍造成断裂.

### 3.5 栅格化和透明度

- 使用预乘 alpha 的面积重采样或 BOX 重采样, 避免透明背景颜色污染边缘.
- 根据 alpha 阈值生成占用掩码, 再删除无意义的小连通分量.
- 不能仅按最近邻把完整照片压到目标尺寸.
- 保留必要孔洞和环形空腔. 清理小连通分量前要确认它们不是尖端, 眼睛高光或中心线.
- 图案占用范围必须满足用户给出的最大宽高. 空白格可以用于把非方形主体放在方形板上.
- 完成颜色量化, 去包边, 对称化和其他逐格修正后, 从最终 CSV 的非空格重新计算外接矩形. 只有这个最终结果可以写入图纸名称中的 `size`.
- 任一结构修改导致外接矩形变化时, 必须同步修改目录名, 全部交付文件名, 元数据和压缩包名.

### 3.6 MARD 色号量化

默认执行以下步骤:

1. 从 `palettes/mard_221_v1.json` 读取所有可用色号和 RGB.
2. 在 Lab 等感知色彩空间中选择紧凑的 MARD 子集.
3. 把每个占用格映射到该子集中的最近色号.
4. 清理零使用色号, 并重新生成材料统计.
5. 检查重要语义色, 例如肤色, 眼睛, 金属, 金色和红色宝石. 不能只以全图平均误差作为判断标准.

只有用户明确指定其他色卡时才切换. 切换时必须保存所用色卡文件或可靠来源, 并在元数据中记录.

### 3.7 结构后处理

量化后逐格检查:

- 尖端是否足够锋利.
- 中心线是否连续. 需要锐利感时, 可从 3格宽过渡到 1格宽, 但过渡必须对称.
- 细剑身, 剑柄, 肢体或连接件是否在同一合理轴线上.
- 主体是否出现非预期断裂或单格悬空.
- 外部包边和背景杂色是否已清除.
- 环, 孔洞和负空间是否仍然可辨认.
- 对称图案是否在最终色号网格上严格对称.

单格细节只有在承担尖端, 中心线, 高光或连接功能时才能保留.

## 4. 必需交付物

每个最终图纸目录至少包含:

- 透明背景的像素预览 `<id>_preview.png`.
- 白底预览 `<id>_preview_white.png`, 如果现有脚本没有生成则补充.
- 带坐标和色号的完整图纸 `<id>_chart.png`.
- 逐格色号表 `<id>.csv`.
- 色号和数量统计 `<id>_inventory.csv`.
- 转换参数与结构约束 `<id>_metadata.json`.
- 分板图目录 `tiles/`, 需要分板时生成.
- 分板图 `tiles/<id>_board_rN_cN.png`.
- 简短的 `README.md`, 标注尺寸, 占用范围, 色卡, 推荐版本和特殊处理.

其中 `<id>` 是完整的 `<name>_<size>_<version>` 图纸标识.

项目级 `masters/` 保存最终使用的母图. `archives/` 保存交付压缩包. 压缩包内不得只包含预览图, 必须包含可实际拼制的图纸和材料统计.

## 5. 元数据要求

元数据至少记录:

- 输入母图路径.
- 图纸标识, 主体名称和版本号.
- 网格尺寸和实际占用范围.
- 板尺寸和分板数量.
- 色卡名称, 色卡路径, 使用色号和总拼豆数.
- alpha 阈值和随机种子.
- 去背景, 去包边, 结构简化和手工修正内容.
- 对称类型, 对称轴或对称中心, 以及最终对称检查结果.
- 对关键细线, 中心线, 尖端, 环或孔洞所做的保护处理.

## 6. 最终验证

交付前必须完成以下检查:

1. 输出网格尺寸与目标一致, 占用宽高没有超限. 目录和文件名中的 `size` 必须等于从最终 CSV 实测的外接矩形.
2. `*_inventory.csv` 的数量之和等于网格中的非空格数.
3. 所有色号都存在于指定色卡. 未指定色卡时必须全部属于 MARD.
4. 透明预览四角和背景区域透明, 无残留底色.
5. 外部包边已去除, 需要的内部结构线仍存在.
6. 轴对称或中心对称结构通过逐格程序校验.
7. 主体连通关系, 孔洞, 尖头和中心线正确.
8. 以 1格对应 1像素的视图检查轮廓, 再查看放大预览和带坐标图纸.
9. 随机打开若干分板图, 核对坐标和色号与 CSV 一致.
10. 将最终推荐版本写入项目 `README.md`.

发现验证失败时, 修正母图或网格并重新生成所有相关交付物. 不得只修预览图而保留错误的 CSV 或材料统计.

## 7. 运行环境

- Python 工作默认使用 conda 环境 `perler-beads`.
- 通用转换入口是 `scripts/make_perler_pattern.py`.
- `scripts/refine_tempus_pattern.py` 是 Tempus 对称化和去外包边的专项后处理示例, 不应直接套用于其他对象.
- 从仓库根目录运行脚本. `--output-root` 指向 `projects/<name>/patterns`, 脚本会在最终外接矩形确定后创建规范名称的图纸目录.
